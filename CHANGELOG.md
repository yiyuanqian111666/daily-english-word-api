# 📄 更新日志 (CHANGELOG)

本文件记录了本项目每一次迭代的重要更新与改进。

---

## [2.0.0] - 2026-03-19 - 架构升级与功能完善

### ✨ 新增功能
- **单词检索接口**：新增 `/api/words/search`，支持通过关键词对单词或释义进行模糊匹配。
- **排行榜分页支持**：`/api/leaderboard` 接口新增 `limit` 和 `offset` 参数，支持安全的高效分页查询。
- **场景与分类过滤**：学习接口支持通过 `category`（单词分类）或 `scene`（对话场景）进行精准内容抽取。

### ⚡ 优化改进
- **架构重构**：全面采用 Flask 的 `g` 对象与 `teardown_appcontext` 机制，实现请求级别的数据库连接复用与生命周期自动管理。
- **安全性增强**：全面使用参数化 SQL 查询，杜绝潜在的 SQL 注入风险，并对排序字段进行了严格的白名单校验。
- **RESTful 规范化**：优化了 HTTP 状态码返回（如数据创建成功返回 `201 Created`，异常返回标准的错误 JSON 和 `400/404/500` 状态码）。

### 🛠 修复与稳定性
- 修复了高并发或多查询下可能出现的数据库连接泄漏问题。
- 完善了输入参数的类型捕获与容错校验，防止非法传参导致后端崩溃。
- 补充并更新了配套的 `unittest` 单元测试套件，全面覆盖各项核心 API。

### 🚀 系统增强
- 提供了一键启动脚本（Linux/macOS 的 `run.sh` 与 Windows 的 `run.bat`），集成自动依赖安装与环境检查。
- 完善了项目各核心生态文档（`README.md`、`MAINTAINERS.md`、`CODE_OF_CONDUCT.md`）。

---

## [1.0.0] - 初始版本
- 实现基础的单词、场景对话学习功能。
- 内置挑战系统与限时评级（SS-F）机制。
- 支持简易的无状态排行榜与扩展数据录入。